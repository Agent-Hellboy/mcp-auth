[project]
name = "mcp-as-client"
version = "0.0.0"
requires-python = ">=3.12"
dependencies = ["mcp-auth-client"]

[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
